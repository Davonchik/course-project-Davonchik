# Reading List Frontend

Фронтенд для сервиса "Reading List" (список книг/статей «к прочтению») на React + TypeScript.

## Возможности

- 🔐 **Аутентификация**: регистрация, логин, автоматическое обновление токенов
- 📚 **Управление записями**: создание, редактирование, удаление записей
- 🔍 **Фильтрация**: по статусу (запланировано, в процессе, завершено)
- 📄 **Пагинация**: удобная навигация по страницам
- 👥 **Роли пользователей**:
  - `user` - видит и управляет только своими записями
  - `admin` - видит и управляет всеми записями, может фильтровать по владельцу
- 📱 **Адаптивный дизайн**: работает на всех устройствах
- ✨ **Современный UI**: красивый и интуитивный интерфейс

## Технологии

- **React 19** с TypeScript
- **React Router** для навигации
- **React Hook Form** для форм
- **Axios** для HTTP запросов
- **CSS3** для стилизации

## Установка и запуск

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` в корне проекта:
```env
REACT_APP_API_URL=http://localhost:8000
```

3. Запустите приложение:
```bash
npm start
```

Приложение будет доступно по адресу `http://localhost:3000`

## Структура проекта

```
src/
├── api/                 # API клиент
│   └── client.ts
├── components/          # React компоненты
│   ├── auth/           # Компоненты аутентификации
│   ├── entries/        # Компоненты записей
│   └── layout/         # Компоненты макета
├── contexts/           # React контексты
│   └── AuthContext.tsx
├── types/              # TypeScript типы
│   └── api.ts
└── App.tsx            # Главный компонент
```

## API интеграция

Приложение интегрировано с FastAPI бэкендом и поддерживает:

- **POST** `/api/v1/auth/register` - регистрация
- **POST** `/api/v1/auth/login` - логин
- **POST** `/api/v1/auth/refresh` - обновление токена
- **GET** `/api/v1/auth/me` - информация о пользователе
- **GET** `/api/v1/entries` - список записей
- **POST** `/api/v1/entries` - создание записи
- **GET** `/api/v1/entries/{id}` - получение записи
- **PATCH** `/api/v1/entries/{id}` - обновление записи
- **DELETE** `/api/v1/entries/{id}` - удаление записи
- **GET** `/api/v1/users` - список пользователей (для админов)

## Особенности

### Автоматическое обновление токенов
Приложение автоматически обновляет access token при истечении срока действия, используя refresh token.

### Строгая валидация
Все формы имеют строгую валидацию:
- Email должен быть корректным
- Пароль минимум 6 символов
- Название записи обязательно
- Ссылка должна быть валидным URL

### Роли и права доступа
- **Пользователи** видят только свои записи
- **Администраторы** видят все записи и могут фильтровать по владельцу
- Все операции проверяют права доступа

### Адаптивный дизайн
Интерфейс адаптируется под разные размеры экранов:
- Десктоп: сетка карточек
- Планшет: адаптивная сетка
- Мобильный: одна колонка

## Разработка

Для сборки продакшн версии:
```bash
npm run build
```

Для запуска тестов:
```bash
npm test
```
