apiVersion: v1
kind: Secret
metadata:
  name: reading-list-secrets
type: Opaque
stringData:
  # DO NOT commit real secrets to git!
  # This is a template file for demonstration purposes only
  # In production, use external secrets management (e.g., Sealed Secrets, External Secrets Operator)
  database-url: "postgresql+asyncpg://postgres:CHANGEME@postgres:5432/reading_list"
  secret-key: "CHANGEME-generate-with-openssl-rand-hex-32"
